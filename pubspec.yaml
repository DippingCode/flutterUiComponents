name: flutterUiComponents
description: Uma coletânea de componentes UIs em Flutter 
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter 
  cupertino_icons: ^1.0.0
  google_fonts: ^1.1.0
  flutter_feather_icons: ^1.0.3
  badges: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter 

flutter:
  uses-material-design: true 

  #assets:

